let op = proc(a, b) {
    if a > b {
        a + b
    } else if a == b { 
        a - b
    } else {
        a * b
    }
};

print op(8, 5);

let func = proc() {
    let a = 10;

    let addten = proc(b) {
        return a++;        
    };

    func()

    return addten;
};

let x = func();